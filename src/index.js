import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <div>Duck chan</div>
  </React.StrictMode>,
  document.getElementById('root')
);
